from math import sqrt

def square(a):
    p = 4 * a
    s = a * a
    d = (a ** 2) / 2
    d = d ** 0.5

    k = (p, s, d)
    return k

# Good seems like it is works but I does not understand what is a, p, s, d, k?
# TODO: rename all variables
# TODO: add annotations for input argument and returning value of function
# TODO: remove not used import
# -1 point for absent docstring and annotations
